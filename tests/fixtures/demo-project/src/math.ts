// 三个结构相同的孪生函数（验证泛化检索 B：astFingerprint 应一致）
export function add(a: number, b: number) {
  return a + b;
}

export function mul(a: number, b: number) {
  return a * b;
}

export function sub(a: number, b: number) {
  return a - b;
}
