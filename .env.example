# 复制为 .env 并填入真实值（.env 已被 gitignore）

# 数据库（本地 Docker 或 Neon/Supabase；pgvector P1 才用）
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/glint?schema=public"

# 模型厂商 Key（MVP 单用户，仅后端使用）
ANTHROPIC_API_KEY=""
OPENAI_API_KEY=""

# data-access 层：mock | real（前端切换数据源）
NEXT_PUBLIC_API_MODE="mock"
